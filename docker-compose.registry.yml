version: '3.8'

services:
  backend:
    image: ${DOCKER_REGISTRY_USER}/sheets-backend:20260119
    restart: always
    expose:
      - "5000"
    env_file:
      - .env

  frontend:
    image: ${DOCKER_REGISTRY_USER}/sheets-frontend:20260119
    restart: always
    ports:
      - "80:8501"
    depends_on:
      - backend
    env_file:
      - .env
